#include "bad.h"

bad::bad(){
	x = random(50, 100);
	y = random(50, 100);
}

int bad::random(int cord, int range){
	srand( time( NULL ) );
	plus = rand() % range;
	plus -= 5;
	cord +=plus;
	return cord;
}

void bad::move(void)